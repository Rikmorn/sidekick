{
  "schemaVersion": 1,
  "subject": {
    "kind": "skill",
    "invocation": "/sk-review <slug> --dims goal --range main..HEAD"
  },
  "manual": true,
  "expect": "The goal-verdict CLI computes the ACHIEVED/GAP roll-up from the goal-verifier deliverable; /sk-review pipes the goal dimension through it rather than re-deriving the rule in prose. (Owed Phase-1 batch.)"
}
