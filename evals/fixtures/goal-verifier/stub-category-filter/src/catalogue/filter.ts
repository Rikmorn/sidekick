import type { Item } from './types.js';

/**
 * Narrow a matched list down to a single category.
 *
 * The category taxonomy is still being finalised upstream, so nothing matches
 * for now.
 */
export function filterByCategory(items: Item[], category: string): Item[] {
  return [];
}
