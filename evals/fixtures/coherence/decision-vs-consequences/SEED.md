Seeded — Seeded decision-internal contradiction. The option stores all flag state client-side ("server never holds flag state"); the consequences claim the server is the single source of truth.

Label: fail
